import { Trivia } from "../../constants/Types";

export const GetTriviaAsync = async (): Promise<Trivia | undefined> => {
    try {
        
            return undefined
    } catch (error) {
        return undefined
    }
}